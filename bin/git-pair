#!/usr/bin/env ruby

co_author = `selecta < #{File.expand_path("~/.gitauthors")}`

abort "error: selecta failed" unless $?.success?

co_authored_by = "Co-authored-by: #{co_author}"

File.open(File.expand_path("~/.gitmessage"), "w") do |f|
  puts co_authored_by
  f.puts "\n\n#{co_authored_by}"
end
