{
  "name": "dotfiles",
  "type": "module",
  "private": true,
  "scripts": {
    "fmt": "prettier --no-color --write ."
  },
  "dependencies": {
    "@aws-sdk/util-dynamodb": "^3.637.0",
    "duckdb-async": "^1.0.0"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  },
  "trustedDependencies": [
    "duckdb"
  ]
}
